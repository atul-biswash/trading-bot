# ---------------------------------------------------------------------------
# Development & test dependencies. Install with: pip install -r requirements-dev.txt
# ---------------------------------------------------------------------------
# Same policy as requirements.txt: anything a quality gate executes is pinned
# exactly, because the gate is an absolute zero rather than a baseline to diff
# against. A floating linter turns the build red on a morning when no code
# changed, and `ruff format`'s output — including the formatter-dirty file count
# recorded in the docs — is itself version-dependent.
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0     # asyncio_mode=auto semantics changed across majors
pytest-cov==7.1.0
pytest-mock==3.15.1
freezegun>=1.4.0          # declared but unused: tests inject a Clock instead
respx>=0.20.2             # declared but unused: nothing calls httpx yet

ruff==0.15.22             # linter + formatter; hard-zero gate
mypy==2.3.0               # static type checking; hard-zero gate
types-PyYAML==6.0.12.20260518   # type stubs
