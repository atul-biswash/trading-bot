# ---------------------------------------------------------------------------
# Development & test dependencies. Install with: pip install -r requirements-dev.txt
# ---------------------------------------------------------------------------
-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
freezegun>=1.4.0          # deterministic time in tests
respx>=0.20.2             # mock httpx calls (Telegram, etc.)

ruff>=0.3.0               # linter + formatter
mypy>=1.8.0               # static type checking
types-PyYAML>=6.0.12      # type stubs
