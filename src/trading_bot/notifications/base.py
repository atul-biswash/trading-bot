"""Notifier base.

Concrete notifiers implement :class:`trading_bot.core.interfaces.Notifier`.
A no-op notifier can be used when notifications are disabled so callers never
need to null-check.

STUB — implemented in the notifications phase.
"""
