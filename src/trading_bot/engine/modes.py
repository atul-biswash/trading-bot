"""Mode wiring.

Assembles the right set of collaborators (exchange client, data source,
executor) for the active TradingMode, so ``live_engine`` stays mode-agnostic.

STUB — implemented in the engine phase.
"""
