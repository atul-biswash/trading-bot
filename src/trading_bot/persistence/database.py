"""Database engine/session management (SQLAlchemy).

Builds the async engine from ``database.url`` and provides session helpers.
SQLite by default; the same code targets Postgres by changing the URL.

STUB — implemented in the persistence phase.
"""
