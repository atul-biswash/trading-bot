"""Historical data fetching and local caching for backtests.

Downloads klines over the REST API (paginated), stores them as Parquet/CSV in
``backtesting.data_dir``, and loads them back as DataFrames.

STUB — implemented in the backtesting phase.
"""
